rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // ユーザーごとのフォルダ
    match /users/{userId}/{allPaths=**} {
      // 認証済みユーザーのみ、自分のフォルダに読み書き可能
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // その他のパスは全て拒否
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
